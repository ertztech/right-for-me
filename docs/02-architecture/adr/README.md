# ADRs

Architecture Decision Records should be added here when a change affects system boundaries, data flow, naming, ownership, or long-term technical direction.

Use an ADR when:

- a planning decision changes architecture
- a feature introduces a durable technical tradeoff
- implementation reveals a pattern worth standardizing

If no ADR exists yet, capture the need during implementation planning or review rather than leaving the decision only in chat.
